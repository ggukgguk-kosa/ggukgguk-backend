<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ggukgguk.api.Member">

   <select id="selectById" parameterType="java.lang.String" resultType="Member">
       <![CDATA[   
         SELECT member_id, member_pw, member_name, member_birth,
         		member_nickname, member_email, member_type, member_comment, member_created_at
         FROM member_tb
         WHERE member_id = #{memberId}         
     	 ]]>
   </select>

</mapper>